# Copy this file to .env. No secrets or private keys are required for the local demo.
# The chain command binds to this loopback HTTP address; demo/gas connect to it.
RPC_URL=http://127.0.0.1:8545

# Separate port for verify:local, which owns and stops its isolated chain.
VERIFY_PORT=18545
